"""
Centralized application settings (env-driven via pydantic-settings): DB URL, JWT secret/expiry, Redis URL, AI-service base URL, CORS origins. Used by: every backend module that needs configuration.

NOTE: Scaffold placeholder only. Implementation logic to be added
during the corresponding roadmap milestone. Do not remove this
file location or name - other modules import from here.
"""
