# ---------------------------------------------------------------------------
# Core Backend dependencies (FastAPI service)
# Install: pip install -r requirements.txt
# ---------------------------------------------------------------------------

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Database / ORM / migrations ---
sqlalchemy==2.0.35
alembic==1.13.2
psycopg2-binary>=2.9.9
geoalchemy2==0.15.2        # PostGIS geography column support (SAD Section 6.2)
pgvector==0.3.4            # pgvector embedding column support (SAD Section 3.3)

# --- Auth / security ---
python-jose[cryptography]==3.3.0   # JWT creation/validation
passlib[bcrypt]==1.7.4             # password hashing
python-multipart==0.0.9            # form/login payloads

# --- Settings / validation ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Background jobs ---
celery==5.4.0
redis==5.0.8

# --- HTTP client (Core Backend -> AI Engine internal calls) ---
httpx==0.27.2

# --- Report generation (SAD Section 19.2) ---
weasyprint==62.3
reportlab>=4.0.0

# --- Dev / test tooling ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
ruff==0.6.9                # lint
black==24.8.0               # format
