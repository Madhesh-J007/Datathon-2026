/**
 * Configured Axios instance: base URL, JWT attach interceptor, refresh-token retry interceptor. Used by: every service file below.
 *
 * NOTE: Scaffold placeholder only. Implementation to be added
 * during the corresponding roadmap milestone.
 */

export {};
