/**
 * Persistent jurisdiction scope selector shown beneath the top bar (SAD Section 4.1). Used by: app shell, every module.
 *
 * NOTE: Scaffold placeholder only. Implementation to be added
 * during the corresponding roadmap milestone.
 */

export default function ContextBar() {
  return null;
}
