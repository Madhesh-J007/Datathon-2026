/**
 * Renders the AI explainability object (top_factors) as a plain-language card - mandatory next to every AI score per SAD Section 15.3. Used by: investigation, predictive modules.
 *
 * NOTE: Scaffold placeholder only. Implementation to be added
 * during the corresponding roadmap milestone.
 */

export default function ExplanationCard() {
  return null;
}
