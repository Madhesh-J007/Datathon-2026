"""
AI Engine settings: model artifact paths, DB read connection (for training data only, never writes to system of record), embedding model name. Used by: every module in ai-engine/.

NOTE: Scaffold placeholder only. Model training/inference logic
to be added during the corresponding roadmap milestone.
"""
